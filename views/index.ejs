<%- include('./partials/header') %>

<div class="container-fluid">
    
    <section class="row justify-content-center">
        <div class="col-sm-8">
            <form class="d-flex flex-column" action='/' method='POST'>
                <div class="form-group">
                    <textarea class="form-control" rows='5' name='content' placeholder="What's on your mind?"></textarea>
                </div>
                <input class="btn btn-outline-primary align-self-end" type='submit' value'Submit' />
            </form>
        </div>
    <% for (let post of posts) { %>
        <div class="col-sm-8">
          <div class="card">
            <div class="card-header">
              <strong><%= post.username %> </strong>
            </div>
            <img class="card-img" src="https://i.imgur.com/9qBZMdF.jpg">
            <div class="card-body">
              <p class="card-text">
                <%= post.content %>
              </p>
            </div>
          </div>
        </div>
    <% } %>
    </section>
</div>

<%- include('./partials/footer') %>